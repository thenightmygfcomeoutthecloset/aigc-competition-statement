---
name: aigc-competition-statement
description: >-
  为大广赛、新媒体节等国内高校竞赛生成 AIGC 创作过程说明书（Word 格式）。
  当用户需要为 AI 辅助创作的参赛作品撰写"创作说明""过程文档"或"AI 声明"时激活本技能。
  涵盖赛事要求核对、提示词与参数补全、工作流截图嵌入、匿名检查和 Word 逐页验收。
---

# AIGC 竞赛创作说明书生成技能

本技能用于为大广赛（全国大学生广告艺术大赛）、大学生新媒体创意节等国内高校创意竞赛，
批量生成符合评审标准的 **AIGC 创作过程说明书**（Word `.docx` 格式）。

---

## 适用场景

- 用户有已完成的 AIGC 参赛作品（海报、IP、视频等），需要配套的"创作过程说明"文档。
- 用户手头有提示词（Prompt）、工作流截图、PSD 图层等素材，但不知道如何组织成文档。
- 用户需要把说明文档和作品打包成压缩包提交。

---

## 第一步：核对赛事要求

1. 询问用户参加的**具体赛事名称和届数**（如"第十届大学生新媒体节"、"第18届大广赛"）。
2. 在用户的竞赛文件夹中查找 `参赛要求.pdf` 或类似文件，提取以下关键信息：
   - AIGC 作品是否需要单独提交创作说明？
   - 说明文档是否有格式、字数或页数限制？
   - 是否需要匿名提交（不得出现姓名、学校等）？
   - 提交形式（压缩包？在线上传？）
3. 如果找不到参赛要求文件，联网搜索该赛事的最新官方要求。
4. **如果赛事没有严格的格式模板**，采用下面的"通用竞赛模板"结构。

---

## 第二步：收集用户素材

在用户的工作目录中查找以下素材，并按类型分类：

### 必需素材
| 类型 | 查找位置 | 用途 |
|---|---|---|
| 最终作品图 | `.jpg` / `.png` 高清大图 | 文档配图和打包提交 |
| 提示词 (Prompt) | `.txt` / `.md` 文件，或 Word 文档中的文本 | 写入"提示词策略"章节 |

### 加分素材（有则嵌入，无则跳过）
| 类型 | 查找位置 | 用途 |
|---|---|---|
| 工作流截图 | ComfyUI workflow `.json` / 截图 `.jpg` `.png` | 嵌入文档证明工具使用 |
| 软件界面截图 | SD WebUI / Photoshop 图层面板截图 | 证明人工后期参与 |
| 草图 / 线稿 | PSD 隐藏图层导出 / 手绘扫描 | 证明前期人工构图 |
| 负面提示词 | Negative Prompt | 写入参数记录 |
| 生成参数 | Steps / CFG / Sampler / Seed / 模型名 | 写入参数记录 |
| PNG 内嵌元数据 | ComfyUI 内嵌 workflow/prompt JSON | 提取可核验参数 |

**提取 PNG 内嵌元数据的方法**：
```python
from PIL import Image
img = Image.open("作品.png")
if img.info.get("workflow"):
    # ComfyUI 内嵌的工作流 JSON
if img.info.get("prompt"):
    # ComfyUI 内嵌的任务配置 JSON
```

---

## 第三步：按模板生成 Word 文档

使用 `python-docx` 生成 Word 文档。**严禁出现以下"一眼 AI"的格式问题**：
- ❌ 灰色背景代码块 / 蓝色超链接字体 / 边框阴影
- ❌ 英文括号标注样式名（如 `(Concept Statement)`）混在中文正文里
- ✅ 全部使用 Word 标准的"正文"和手动加粗的标题，白纸黑字

### 通用竞赛模板结构

```
【作品名称】AIGC 创作过程说明书

一、 创意阐释与设计逻辑
  1. 创作初衷
     - 选题背景、为什么选这个主题
  2. 设计逻辑
     - 视觉风格选择的理由、构图策略

二、 AI 创作报告与人机协同
  1. 核心工具链
     - 列出使用的工具（ComfyUI / SD / Midjourney / ChatGPT 等）
     - 说明各工具在流程中的角色
  2. 提示词策略
     - 摘录核心正向提示词（中英文均可）
     - 如有负向提示词，一并列出
     - 简述提示词的构建逻辑（风格控制、色彩约束、构图约束等）
  3. 完整参数记录（如果有）
     - 正向 Prompt 全文
     - 负向 Prompt 全文
     - 模型名 / LoRA / ControlNet / Steps / CFG / Sampler / Seed
     - 放大参数（如 SeedVR2 / ESRGAN 等）
  4. 迭代优化与难点攻克
     - V1 生成了什么问题？
     - V2 如何通过修改提示词/参数解决？
     - 体现"精准控制 AI"的能力
  5. 人机协同
     - AI 负责了哪些部分？（占比 %）
     - 人工负责了哪些部分？（占比 %）
     - 具体说明人工干预内容（手绘线稿、修复畸变、排版、品牌嵌入等）

三、 创作过程截图证明
  - 嵌入过程对比图（草图 → V1 → V2 → 最终成品）
  - 嵌入工作流截图（ComfyUI 节点连线 / SD WebUI 界面）
  - 嵌入 Photoshop 图层面板截图（证明人工后期）
  - 每张图下方加一行简短图注说明
```

### 关键写作原则

1. **语气自然**：像一个认真做项目的大学生写的复盘报告，不要像 AI 生成的论文。
2. **中英文混排**：提示词保留英文原文，正文用中文。
3. **诚实但扬长**：如实说明 AI 的参与，但重点突出人工主导的部分（前期构思、后期精修）。
4. **人机比例建议**：一般写 AI 60-70%、人工 30-40%，根据实际情况调整。
5. **不要编造**：不知道的参数就不写，不要捏造 Seed 或模型名。

---

## 第四步：匿名检查

在生成的 Word 文档中执行以下检查：

```python
# 检查正文中是否包含个人信息
keywords = ['姓名', '学号', '学校', '学院', '院系', '指导教师', '导师']
for para in doc.paragraphs:
    for kw in keywords:
        if kw in para.text:
            # 警告用户并删除
```

同时检查 Word 文件的 `core_properties`（作者、公司等元数据字段），必要时清空。

---

## 第五步：打包压缩

将说明文档和作品原图打包成 ZIP 压缩包：

```python
import zipfile
with zipfile.ZipFile('作品名_参赛提交包.zip', 'w', zipfile.ZIP_DEFLATED) as z:
    z.write('AIGC创作说明书.docx', 'AIGC创作说明书.docx')
    for img in 作品图片列表:
        z.write(img, img.name)
```

---

## 第六步：最终验收清单

在交付给用户前，逐项确认：

- [ ] Word 文档能正常打开，无乱码
- [ ] 正文中无"一眼 AI"的格式（无灰色框、无蓝字、无边框）
- [ ] 正文中无"复现""reconstruction"等不当措辞
- [ ] 正文中无个人身份信息（如赛事要求匿名）
- [ ] 创意说明部分不少于 200 字
- [ ] 提示词部分完整收录了实际使用的 Prompt
- [ ] 至少嵌入了 1 张过程对比截图
- [ ] 如有工作流截图 / PS 图层截图，已嵌入
- [ ] 参数记录（如有）与实际工具和文件一致，无捏造
- [ ] 图注简洁明了
- [ ] ZIP 压缩包内包含说明文档 + 全部作品原图
- [ ] 压缩包可正常解压

---

## 常见赛事速查

| 赛事 | AIGC 说明要求要点 |
|---|---|
| **大广赛（全国大学生广告艺术大赛）** | 需在作品中标注使用了 AI；文案类需附"AI 工具辅助说明"；平面/视频类建议附创作过程 |
| **大学生新媒体创意节** | AIGC 类需附"创作过程说明"，包含制作过程和参数；无严格格式要求，和作品一起打包提交 |
| **学院奖** | 需在报名时勾选"使用了 AIGC 工具"；建议附创作说明 |
| **全国大学生数字媒体科技作品及创意竞赛** | 需附技术报告，说明 AI 参与环节 |

> 如果用户的赛事不在以上列表中，联网搜索最新要求。
