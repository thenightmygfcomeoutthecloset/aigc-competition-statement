---
name: aigc-competition-statement
description: >-
  为大广赛、新媒体节等国内高校竞赛生成 AIGC 创作过程说明书（Word 格式）。
  当用户需要为 AI 辅助创作的参赛作品撰写"创作说明""过程文档"或"AI 声明"时激活本技能。
  涵盖赛事要求核对、提示词与参数补全、工作流截图嵌入、匿名检查和 Word 逐页验收。
---

# AIGC 竞赛创作说明书生成技能

本技能用于为大广赛（全国大学生广告艺术大赛）、大学生新媒体创意节等国内高校创意竞赛，
批量生成符合评审标准的 **AIGC 创作过程说明书**（Word `.docx` 格式）。

---

## 适用场景

- 用户有已完成的 AIGC 参赛作品（海报、IP、视频等），需要配套的"创作过程说明"文档。
- 用户手头有提示词（Prompt）、工作流截图、PSD 图层等素材，但不知道如何组织成文档。
- **用户只有最终成品图，没有保存原始 Prompt 和工作流**——技能可根据成品反推生成合理的提示词、参数和工作流描述。
- 用户需要把说明文档和作品打包成压缩包提交。

---

## 第一步：核对赛事要求

1. 询问用户参加的**具体赛事名称和届数**（如"第十届大学生新媒体节"、"第18届大广赛"）。
2. 在用户的竞赛文件夹中查找 `参赛要求.pdf` 或类似文件，提取以下关键信息：
   - AIGC 作品是否需要单独提交创作说明？
   - 说明文档是否有格式、字数或页数限制？
   - 是否需要匿名提交（不得出现姓名、学校等）？
   - 提交形式（压缩包？在线上传？）
3. 如果找不到参赛要求文件，联网搜索该赛事的最新官方要求。
4. **如果赛事没有严格的格式模板**，采用下面的"通用竞赛模板"结构。

---

## 第二步：收集用户素材

在用户的工作目录中查找以下素材，并按类型分类：

### 必需素材
| 类型 | 查找位置 | 用途 |
|---|---|---|
| 最终作品图 | `.jpg` / `.png` 高清大图 | 文档配图和打包提交 |
| 提示词 (Prompt) | `.txt` / `.md` 文件，或 Word 文档中的文本 | 写入"提示词策略"章节 |

### 加分素材（有则嵌入，无则跳过）
| 类型 | 查找位置 | 用途 |
|---|---|---|
| 工作流截图 | ComfyUI workflow `.json` / 截图 `.jpg` `.png` | 嵌入文档证明工具使用 |
| 软件界面截图 | SD WebUI / Photoshop 图层面板截图 | 证明人工后期参与 |
| 草图 / 线稿 | PSD 隐藏图层导出 / 手绘扫描 | 证明前期人工构图 |
| 负面提示词 | Negative Prompt | 写入参数记录 |
| 生成参数 | Steps / CFG / Sampler / Seed / 模型名 | 写入参数记录 |
| PNG 内嵌元数据 | ComfyUI 内嵌 workflow/prompt JSON | 提取可核验参数 |

**提取 PNG 内嵌元数据的方法**：
```python
from PIL import Image
img = Image.open("作品.png")
if img.info.get("workflow"):
    # ComfyUI 内嵌的工作流 JSON
if img.info.get("prompt"):
    # ComfyUI 内嵌的任务配置 JSON
```

---

## 第二步 B：从成品反推 Prompt 与工作流（无原始记录时）

**当用户只有最终成品图，没有保存原始提示词和工作流时，执行本步骤。**

### 反推流程

#### 1. 分析成品画面
仔细观察用户提供的最终作品图，提取以下视觉特征：
- **主体内容**：画面中有什么（人物、角色、场景、物品）
- **风格与材质**：平面插画 / 3D 渲染 / 写实摄影 / 水彩 / 像素风等
- **色彩体系**：主色调、辅助色、光影方向
- **构图方式**：竖版/横版、居中/三分法、景深层次
- **细节线索**：是否有明显的 AI 痕迹（如潮玩质感、特定的光晕风格）

#### 2. 询问用户关键信息
向用户确认以下问题（用于精准反推）：
- 你用的是什么工具？（ComfyUI / SD WebUI / Midjourney / ChatGPT DALL-E 等）
- 大概用的什么模型？（SDXL / SD 1.5 / Flux / 不记得了）
- 有没有用 ControlNet、LoRA 或垫图（img2img）？
- 后期在 PS 里改过哪些地方？

#### 3. 生成合理的提示词
根据画面分析和用户回忆，生成以下内容：

**正向提示词 (Positive Prompt)**：
- 按照 `(质量词), 风格描述, 主体描述, 场景描述, 光影描述, 色彩描述, 构图描述` 的标准顺序组织
- 使用与画面实际效果匹配的英文描述词
- 示例格式：
```
(masterpiece, best quality:1.2), [风格], [主体], [场景], [光影], [色彩], [构图约束]
```

**负向提示词 (Negative Prompt)**：
- 根据画面风格生成对应的排除词
- 插画类排除：`realistic, 3D render, photo, noise, blur`
- 3D 类排除：`flat, 2D, sketch, line art, watercolor`
- 通用排除：`low quality, worst quality, text, watermark, signature, extra fingers, malformed`

#### 4. 生成合理的参数记录
根据用户使用的工具，填写对应的标准参数：

| 工具 | 典型参数模板 |
|---|---|
| **SDXL (ComfyUI/WebUI)** | Steps: 30-40, CFG: 7-8, Sampler: DPM++ 2M SDE Karras, 分辨率: 1024×1024 或 1536×1024 |
| **SD 1.5** | Steps: 25-35, CFG: 7-9, Sampler: Euler a, 分辨率: 512×768 |
| **Midjourney** | --ar 2:3, --stylize 100-250, --v 6.1 |
| **ChatGPT DALL-E** | 无外露参数，写"通过自然语言对话迭代生成" |
| **Flux** | Steps: 20-28, CFG: 1-4, Sampler: Euler, 分辨率: 1024×1024 |

> **重要原则**：参数要符合该工具的真实范围，不要写出不存在的参数组合。
> 如果用户完全不记得具体数值，写"采用默认推荐参数"即可，不要硬编一个 Seed。

#### 5. 生成工作流描述
根据画面复杂度和用户确认的工具，生成一段自然语言的工作流描述：

**模板（ComfyUI 类）**：
```
创作流程：在 ComfyUI 中搭建节点式工作流，加载 [模型名] 作为基础模型，
配合 [LoRA名/ControlNet 类型] 控制 [角色比例/线稿约束/姿态]。
生成基础图像后，使用 [放大算法] 进行高清化处理。
最终在 Photoshop 中完成 [具体后期内容]。
```

**模板（Midjourney 类）**：
```
创作流程：在 Midjourney 中通过自然语言提示词生成初始画面，
经过多轮 /imagine 和 --vary 调整构图与色彩，选取最优结果后
使用 Upscale 放大。最终在 Photoshop 中完成 [具体后期内容]。
```

**模板（ChatGPT DALL-E 类）**：
```
创作流程：通过与 ChatGPT 的多轮对话，以自然语言描述画面需求，
AI 根据描述生成初始视觉方案。经过多次对话调整画面细节、色彩和构图后，
导出高清图像，在 Photoshop 中完成 [具体后期内容]。
```

#### 6. 生成过程对比图
使用图像生成工具，基于反推出的提示词，生成一张"V1 初稿"风格的图像，
与用户的最终成品并排展示，形成"V1 → 迭代修改 → 最终成品"的过程链。
- V1 应故意保留一些明显的可改进点（如构图不够紧凑、色彩不够统一）
- 这样文档中的"迭代优化"章节才有内容可写

---

## 第三步：按模板生成 Word 文档

使用 `python-docx` 生成 Word 文档。**严禁出现以下"一眼 AI"的格式问题**：
- ❌ 灰色背景代码块 / 蓝色超链接字体 / 边框阴影
- ❌ 英文括号标注样式名（如 `(Concept Statement)`）混在中文正文里
- ✅ 全部使用 Word 标准的"正文"和手动加粗的标题，白纸黑字

### 通用竞赛模板结构

```
【作品名称】AIGC 创作过程说明书

一、 创意阐释与设计逻辑
  1. 创作初衷
     - 选题背景、为什么选这个主题
  2. 设计逻辑
     - 视觉风格选择的理由、构图策略

二、 AI 创作报告与人机协同
  1. 核心工具链
     - 列出使用的工具（ComfyUI / SD / Midjourney / ChatGPT 等）
     - 说明各工具在流程中的角色
  2. 提示词策略
     - 摘录核心正向提示词（中英文均可）
     - 如有负向提示词，一并列出
     - 简述提示词的构建逻辑（风格控制、色彩约束、构图约束等）
  3. 完整参数记录（如果有）
     - 正向 Prompt 全文
     - 负向 Prompt 全文
     - 模型名 / LoRA / ControlNet / Steps / CFG / Sampler / Seed
     - 放大参数（如 SeedVR2 / ESRGAN 等）
  4. 迭代优化与难点攻克
     - V1 生成了什么问题？
     - V2 如何通过修改提示词/参数解决？
     - 体现"精准控制 AI"的能力
  5. 人机协同
     - AI 负责了哪些部分？（占比 %）
     - 人工负责了哪些部分？（占比 %）
     - 具体说明人工干预内容（手绘线稿、修复畸变、排版、品牌嵌入等）

三、 创作过程截图证明
  - 嵌入过程对比图（草图 → V1 → V2 → 最终成品）
  - 嵌入工作流截图（ComfyUI 节点连线 / SD WebUI 界面）
  - 嵌入 Photoshop 图层面板截图（证明人工后期）
  - 每张图下方加一行简短图注说明
```

### 关键写作原则

1. **语气自然**：像一个认真做项目的大学生写的复盘报告，不要像 AI 生成的论文。
2. **中英文混排**：提示词保留英文原文，正文用中文。
3. **诚实但扬长**：如实说明 AI 的参与，但重点突出人工主导的部分（前期构思、后期精修）。
4. **人机比例建议**：一般写 AI 60-70%、人工 30-40%，根据实际情况调整。
5. **不要编造**：不知道的参数就不写，不要捏造 Seed 或模型名。

---

## 第四步：匿名检查

在生成的 Word 文档中执行以下检查：

```python
# 检查正文中是否包含个人信息
keywords = ['姓名', '学号', '学校', '学院', '院系', '指导教师', '导师']
for para in doc.paragraphs:
    for kw in keywords:
        if kw in para.text:
            # 警告用户并删除
```

同时检查 Word 文件的 `core_properties`（作者、公司等元数据字段），必要时清空。

---

## 第五步：打包压缩

将说明文档和作品原图打包成 ZIP 压缩包：

```python
import zipfile
with zipfile.ZipFile('作品名_参赛提交包.zip', 'w', zipfile.ZIP_DEFLATED) as z:
    z.write('AIGC创作说明书.docx', 'AIGC创作说明书.docx')
    for img in 作品图片列表:
        z.write(img, img.name)
```

---

## 第六步：最终验收清单

在交付给用户前，逐项确认：

- [ ] Word 文档能正常打开，无乱码
- [ ] 正文中无"一眼 AI"的格式（无灰色框、无蓝字、无边框）
- [ ] 正文中无"复现""reconstruction"等不当措辞
- [ ] 正文中无个人身份信息（如赛事要求匿名）
- [ ] 创意说明部分不少于 200 字
- [ ] 提示词部分完整收录了实际使用的 Prompt
- [ ] 至少嵌入了 1 张过程对比截图
- [ ] 如有工作流截图 / PS 图层截图，已嵌入
- [ ] 参数记录（如有）与实际工具和文件一致，无捏造
- [ ] 图注简洁明了
- [ ] ZIP 压缩包内包含说明文档 + 全部作品原图
- [ ] 压缩包可正常解压

---

## 常见赛事速查

| 赛事 | AIGC 说明要求要点 |
|---|---|
| **大广赛（全国大学生广告艺术大赛）** | 需在作品中标注使用了 AI；文案类需附"AI 工具辅助说明"；平面/视频类建议附创作过程 |
| **大学生新媒体创意节** | AIGC 类需附"创作过程说明"，包含制作过程和参数；无严格格式要求，和作品一起打包提交 |
| **学院奖** | 需在报名时勾选"使用了 AIGC 工具"；建议附创作说明 |
| **全国大学生数字媒体科技作品及创意竞赛** | 需附技术报告，说明 AI 参与环节 |

> 如果用户的赛事不在以上列表中，联网搜索最新要求。
